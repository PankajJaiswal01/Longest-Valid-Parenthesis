Step 1. Start by take string as input.
Step 2. Parse through each character of string .
Step 3. During parsing push character of string if it is '('. If character is ')' then go and check top element of stack and if it is not empty and equals to '(' then pop it from stack alongwith increasing counter.
Step 4. After parsing wholestring, answer is in counter.
step 5. Successfully completed.
